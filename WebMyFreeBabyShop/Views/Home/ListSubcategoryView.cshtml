@model IEnumerable<WebMyFreeBabyShop.Models.Subcategory>

@{
    ViewBag.Title = "ListCategory";
}

<h2>ListCategory</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.subcategory1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.subcategory1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSubcategory }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSubcategory }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSubcategory })
        </td>
    </tr>
}

</table>
